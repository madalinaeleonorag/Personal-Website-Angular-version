import { ProgressBarColorsDirective } from './progress-bar-colors.directive';

describe('ProgressBarColorsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressBarColorsDirective();
    expect(directive).toBeTruthy();
  });
});
